{
  "name": "static-file-manager",
  "version": "1.0.0",
  "description": "Static file manager with login",
  "scripts": {
    "start": "node -e \"require('serve').default('.', { port: process.env.PORT })\""
  },
  "dependencies": {
    "serve": "^14.2.5"
  }
}